import { PersonalAptoTemporada } from './personal-apto-temporada';

describe('PersonalAptoTemporada', () => {
  it('should create an instance', () => {
    expect(new PersonalAptoTemporada()).toBeTruthy();
  });
});
