import { RespuestasPersonal } from './respuestas-personal';

describe('RespuestasPersonal', () => {
  it('should create an instance', () => {
    expect(new RespuestasPersonal()).toBeTruthy();
  });
});
